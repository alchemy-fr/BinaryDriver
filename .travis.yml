language: php

sudo: false

matrix:
  fast_finish: true
  allow_failures:
    - php: 7.0
    - php: hhvm

before_script:
  - composer self-update
  - composer install --dev --prefer-source

php:
  - 5.5
  - 5.6
  - 7.0
  - hhvm
